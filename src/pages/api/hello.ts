// Next.js API route support: https://nextjs.org/docs/api-routes/introduction
import type { NextApiRequest, NextApiResponse } from "next";
// import { createUser, getUsers } from "@/server/services/user";
// import { createPost, getPosts } from "@/server/services/post";

export default async function handler(
  req: NextApiRequest,
  res: NextApiResponse,
) {

  // const user = await createUser('John Doe', 'correo5@corre.com')
  // const users = await getUsers()

  // const post = await createPost('Post 1', 'Contenido del post', user.id)

  // const posts = await getPosts({ authorID: '53ebd608-328e-44a3-8a89-e8d9007cefe7' })

  // console.log({ users })
  // return res.status(200).json({ posts });
}
